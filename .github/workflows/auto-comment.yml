name: Auto Comment on Issues (disabled duplicate)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Duplicate workflow disabled
        run: echo "This duplicate workflow has been disabled. Use issue-comment.yml for automatic issue comments."
